# Öffentliche Build-Defaults (Next.js lädt diese bei `next build` / Production).
# Überschreibbar via .env.production.local (nicht committet).
NEXT_PUBLIC_SITE_URL=https://ivo-tech.com
NEXT_PUBLIC_APP_ENV=production
